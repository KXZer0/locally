"""Questions about a model directory, answered without loading it.

    identity   vision or text, generative or audio, and its name
    geometry   context length, KV bytes/token, MoE expert share
    irinfo     the IR's own rt_info: real weight precision
    integrity  is the .bin actually complete
    describe   all of the above, for --scan
"""
