"""Tool calling, both directions.

    render     schemas -> prompt text the model will answer in
    parse      whatever the model emitted -> structured calls
    text       stripping the machinery back out of prose
"""
